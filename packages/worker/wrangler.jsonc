/**
 * For more details on how to configure Wrangler, refer to:
 * https://developers.cloudflare.com/workers/wrangler/configuration/
 */
{
	"$schema": "node_modules/wrangler/config-schema.json",
	"name": "file-share",
	"main": "src/index.ts",
	"compatibility_date": "2026-06-04",
	"routes": [
		{
			"pattern": "fs.sekkki.com",
			"custom_domain": true
		}
	],
	"assets": {
		"binding": "ASSETS",
		"directory": "./public"
	},
	"observability": {
		"enabled": true
	},
	"upload_source_maps": true,
	"compatibility_flags": [
		"nodejs_compat"
	],
	// ── R2 文件存储 ──
	"r2_buckets": [
		{
			"binding": "FILE_BUCKET",
			"bucket_name": "file-share-files"
		}
	],
	// ── KV 元数据存储 ──
	"kv_namespaces": [
		{
			"binding": "FILE_KV",
			"id": "a85ae9ea5b074aec81a3325f99f96021",
		}
	],
	// ── Durable Objects（WebRTC 信令） ──
	"durable_objects": {
		"bindings": [
			{
				"name": "SIGNALING",
				"class_name": "SignalingDO"
			}
		]
	},
	"migrations": [
		{
			"tag": "v1",
			"new_classes": [
				"SignalingDO"
			]
		}
	],
	// ── 环境变量 ──
	"vars": {
		"ADMIN_JWT_SECRET": "dev-placeholder"
	},
	// ── 定时清理任务 ──
	"triggers": {
		"crons": [
			"*/5 * * * *"
		]
	},
	// ── 本地开发配置 ──
	"dev": {
		"host": "localhost"
	}
}